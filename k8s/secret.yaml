apiVersion: v1
kind: Secret
metadata:
  name: agent-factory-secrets
  namespace: agent-factory
type: Opaque
stringData:
  OPENAI_API_KEY: ""  # Set via kubectl or sealed-secrets
  ANTHROPIC_API_KEY: ""
  POSTGRES_USER: "agent_factory"
  POSTGRES_PASSWORD: ""  # Set via kubectl or sealed-secrets
  JWT_SECRET_KEY: ""  # Set via kubectl or sealed-secrets
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
